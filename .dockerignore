# This repository only ships the Dockerfile and the workflow: the simple-jev
# source is cloned inside the image at build time. Keeping the build context to
# just the Dockerfile makes every build upload a few kilobytes instead of the
# whole working tree.
**
!Dockerfile
!.dockerignore
